# DeepSeek Harness 桌面版 · 交接文档

> 交接日期：2026-08-18
> 环境：Windows 10/11 x64（本机 win32 10.0.26200），Git Bash / PowerShell

---

## 1. 项目概览

本工作包含**两个部分**：

| 部分 | 路径 | 说明 |
|------|------|------|
| DeepSeek Harness 源码仓库 | `C:\Epan\DS Harness\deepseek-harness` | 官方仓库 clone，用于源码运行（`pnpm dsh web`） |
| **桌面版应用工程** | `C:\Epan\DS Harness\dsh-desktop` | **本次主要交付物**：Electron 壳，把 dsh 打包成独立 Windows .exe |

**交付目标**：双击 .exe 即可启动 dsh 服务并弹出独立桌面窗口（不经浏览器、不开终端）。

### 最终产物（`dsh-desktop\dist\`）

| 文件 | 大小 | 用途 |
|------|------|------|
| `DeepSeek Harness Setup 0.1.0.exe` | ~136 MB | NSIS 安装程序（向导式、可改安装目录） |
| `DeepSeek-Harness-0.1.0-portable.exe` | ~136 MB | 免安装便携版，双击即用 |
| `win-unpacked\` | ~555 MB | 解包目录（= 安装后的内容，开发验证用） |

---

## 2. 环境与关键版本

| 组件 | 版本 | 说明 |
|------|------|------|
| Node.js | 24.16.0 | `C:\Program Files\nodejs` |
| npm | 11.13.0 | 全局包安装用 |
| **pnpm** | **11.7.0** | 全局安装（`npm i -g pnpm@11.7.0`），与仓库 `packageManager` 声明一致 |
| Electron | 43.4.0 | 桌面壳运行时 |
| electron-builder | 26.15.3 | 打包工具 |
| @deepseek-ai/dsh | 0.1.0-rc.6 | npm 官方 dsh 包（桌面版内嵌） |

### 系统级修复（已完成，一次性的）

1. **安装 pnpm**：`npm install -g pnpm@11.7.0`（装在用户目录 `C:\Users\林~\AppData\Roaming\npm`，无需管理员权限）。
2. **PowerShell 执行策略**：`Set-ExecutionPolicy -Scope CurrentUser RemoteSigned`（默认 Restricted 会禁止运行 pnpm.ps1）。当前用户已永久生效，新终端即用。
3. **环境残留清理**：无旧版 dsh 残留。注意 `C:\Users\林~\.codex\skills\easyeda-api\...` 的 node.exe 是 Codex 桥接服务，与 dsh 无关，**勿动**。

---

## 3. 桌面应用架构（Electron 壳工作原理）

```
DeepSeek Harness.exe（主进程，main.js）
  │  spawn(process.execPath, ['--expose-internals', bin.js, 'web', '--host', '127.0.0.1', '--port', '0'])
  │  env 注入 ELECTRON_RUN_AS_NODE=1   ← 关键
  ▼
dsh web 服务子进程（内置 Node 运行，随机空闲端口）
  │  打印 "dsh web: http://127.0.0.1:<PORT>"
  ▼
主进程解析端口 → 轮询 HTTP 就绪 → BrowserWindow 加载 http://127.0.0.1:<PORT>
```

要点：
- **服务用 Electron 自带的 Node 跑**（`ELECTRON_RUN_AS_NODE=1` + `process.execPath`），应用自包含，不依赖系统 node。
- **`--port 0`** 让服务自动选空闲端口，避免端口冲突。
- **单实例锁**：`app.requestSingleInstanceLock()`，重复启动会聚焦已有窗口。
- **关窗即停服**：`window-all-closed → app.quit() → will-quit 杀掉服务子进程`。
- **无自带浏览器行为**：dsh web 本身不会自动打开浏览器（源码无 open-browser 逻辑），窗口完全由壳接管。

---

## 4. 踩坑与修复记录（重要，勿回退）

### 4.1 `--expose-internals`（必须）
- **症状**：服务启动报 `failed to apply loader entry ... (@deepseek-ai/cordis-plugin-hmr): --expose-internals is required`。
- **原因**：web profile 的 HMR 插件需要 Node 的 `--expose-internals` 才能访问内部 loader（`loader/internal.js` 检查 `process.execArgv`）。
- **修复**：spawn 参数第一位加 `--expose-internals`。

### 4.2 asar 打包导致符号链接死链（必须 `asar: false`）
- **症状**：打包版服务报 `Cannot find package '@deepseek-ai/dsh-web-app' imported from ~/.dsh/profiles/web`。
- **原因**：dsh 把插件包以**符号链接**装进 `~/.dsh/profiles/node_modules`，链接指向 dsh 包自身 node_modules。打包用 asar 时链接指向 `app.asar/内部路径`，**操作系统无法穿越 asar 文件** → 全部坏链。
- **修复**：`package.json → build.asar: false`（应用打成真实目录 `resources/app/`）。这也是为什么桌面版**不能**开 asar。

### 4.3 僵尸 electron 进程 / 单实例锁（环境问题，注意排查）
- **症状**：exe 静默退出（exit 0），无任何日志；或点击图标"没反应"。
- **原因**：之前测试的 electron 进程被任务管理器杀掉 wrapper 后，子进程成孤儿存活，占着**同一个 userData 的单实例锁**；新实例 `requestSingleInstanceLock()` 失败 → 直接退出。
- **排查**：`tasklist | grep -i electron`；清理：`taskkill //IM electron.exe //F`、`taskkill //IM "DeepSeek Harness.exe" //F`。
- **教训**：GUI 程序的 stdout 在打包后不可见，调试时给 main.js 加**文件日志**（写 `os.tmpdir()`；`C:\` 根目录可能不可写，别用）。

### 4.4 electron-builder 漏打包 peerDependencies（最重要的一次修复）
- **症状**：安装版报 `ERR_MODULE_NOT_FOUND: Cannot find package '@deepseek-ai/cordis-plugin-group'`。
- **原因**：dsh 生态大量用 **`peerDependencies`** 声明运行时依赖（npm 会安装，但 **electron-builder 只打包 `dependencies`，完全忽略 peers**）。
- **假象**：win-unpacked 测试"通过"是因为它的路径向上能**泄露**进 `dsh-desktop\node_modules`（完整依赖树），安装到 `%LOCALAPPDATA%\Programs\...` 后没有泄露通道就崩。
- **修复**：脚本汇总整个生产依赖闭包里所有包的 `peerDependencies`（共 **112 个**），全部写入根 `package.json` 的 `dependencies`（扁平化），强制 electron-builder 打包。
- **验证**：`comm -23 <(ls node_modules/@deepseek-ai) <(ls win-unpacked/resources/app/node_modules/@deepseek-ai)` 应为空；**隔离环境测试**（复制到无父级 node_modules 的路径运行）通过。

### 4.5 目录选择器（win32 folder dialog worker）
- **症状**：选择工作区报 `directory picker failed: win32 folder dialog worker exited before reporting a result`。
- **机制**：`dsh-host-directory-picker-native` 用 `spawn(process.execPath, [worker.cjs])` 起子进程弹系统文件夹对话框，worker 通过 IPC 报告结果；**worker 必须以纯 Node 模式运行**（`ELECTRON_RUN_AS_NODE=1`），否则会启动整个应用的"第二实例"→ 单实例锁失败 → exit 0 → 无消息 → 报此错。
- **修复**：无代码改动 —— 壳给服务注入的 `ELECTRON_RUN_AS_NODE=1` 会沿 env 传给 worker（继承链：main.js → 服务 → worker）。
- **排查结论**：经 API 直连实测（`host.pickDirectory`），当前运行实例的 picker **工作正常**；此前失败与当时 profile 符号链接停留在旧版坏链状态有关，新版启动时已自动修复。
- **验证方法**：`POST /api/host.pickDirectory`，请求挂起 = 对话框正常弹出；立即返回错误 = worker 崩了。

### 4.6 图标
- 用官方 favicon（`deepseek-harness\apps\web\public\favicon.svg`）生成白底黑标多尺寸 ICO（16~256px），存 `build\icon.ico`。
- 生成脚本 `build-icon.mjs` 已留档，可随时换图标重新生成。
- 验证：从 exe 提取图标与 logo 渲染对比 **100% 一致**。

---

### 4.7 模型目录：内置 DeepSeek-Flash + 启动自动同步

- **背景**：`GET https://api.deepseek.com/models` 当前只返回 `deepseek-flash` 与 `deepseek-v4-pro`；`deepseek-v4-flash` 已退化为服务端别名（实测用 `deepseek-v4-flash` 请求，响应里的 `model` 字段回的是 `deepseek-flash`）。而 `@deepseek-ai/dsh-llm-deepseek` 内置的 `DEFAULT_MODELS` 还是改名前的两份，模型选择器里看不到最新的 Flash。
- **两层机制**（互为兜底）：
  1. **内置兜底目录**：`patches/dsh-llm-deepseek/flash-model.cjs` 是源码改写型 patch，向 `node_modules/@deepseek-ai/dsh-llm-deepseek/lib/index.js` 的 `DEFAULT_MODELS` 头部插入 `deepseek-flash`。离线、首次启动、`settings.yaml` 缺失时都由它兜底。`scripts/apply-patches.cjs` 为此新增了 `transforms` 流程（既有三条仍是整文件替换），重复执行幂等，找不到锚点会报错退出。
  2. **启动自动同步**：`scripts/model-sync.cjs` 由 `main.js` 在 `app.whenReady()` 里 **fire-and-forget** 调用（不 await，窗口不等网络）。每次启动拉 `{baseURL}/models`，与 `$DSH_HOME/settings.yaml` 的 `llm-deepseek.models` 比对，不同就把该列表**整体改写**：新增的出现、下架的消失。settings 段是热加载的，改完**不用重启**。
- **api/key 未改动**：新条目仍挂在 `deepseek-official` 路由下，沿用同一个 `baseURL`（https://api.deepseek.com）与同一份 `DEEPSEEK_API_KEY` 凭据（环境变量优先，其次 `~/.dsh/.credentials.yaml`）。catalog 只是"建议清单"，未列出的模型 id 本来也会原样透传。
- **保守行为**：`settings.yaml` 不存在 / 没有 key / 接口非 2xx / 返回空列表 → **一律保持现有目录**；只重写 `models:` 子块，`llm-deepseek:` 的兄弟键（`baseURL`、`apiKeyEnv`…）与其它顶层段落逐字节保留；改动前留 `settings.yaml.bak-model-sync` 备份；写入走临时文件 + rename，避免读到半个文件。
- **可观测**：GUI 看不到 stdout，所以同步结果同时写 `$DSH_HOME/model-sync.log`（超过 64KB 自动清空）。
- **退出自动同步**：删掉 `settings.yaml` 里的 `llm-deepseek:` 段即可退回内置目录。
- **打包依赖**：`model-sync.cjs` 必须进 exe，故 `package.json → build.files` 增加了 `scripts/**/*`（并用 `!scripts/*.test.cjs` 排除测试）。`main.js` 的 require 套了 try/catch：万一打包漏了只会退化成"不自动同步"，不会让应用起不来。
- **验证**：
  - 单测：`node scripts/model-sync.test.cjs`（自带 fixture，只写系统临时目录，不碰真实配置）。
  - 线上：`DSH_PORT=<端口> node scripts/dsh-api.mjs llm.models '{}'`，看 `groups[deepseek-official].models`。

---

### 4.8 目录选择器"没反应"：对话框缺 owner（本次定位的真凶）

- **症状**：点"选择目录"看不到系统文件夹对话框（偶尔闪一下），UI 完全没反应；有时又是好的，时好时坏。
- **先看客户端怎么处理**：`dsh-client-ui-directory-picker-native/lib/client.js` 里 `pick()` 返回 `path === null` 时调 `onCancel()` —— **什么都不做**；只有抛错才走 `onError`（显示错误）。所以"没反应" = 选择器**没弹窗就返回了"取消"**。
- **真凶**：`dsh-host-directory-picker-native/lib/worker.cjs` 里 `IFileDialog::Show` 的 owner 参数传的是 `null`。**无 owner 的对话框，Windows 不保证给前台**，于是它弹在 Harness 窗口后面（窗口 1400×900，正好能把对话框完全遮住）→ 用户以为没反应。vendor 注释还写着"它是子进程的第一个窗口，Windows 会自动激活"，实测不成立。
- **实测证据**（本机，前台是别的程序时）：
  - 补丁前：对话框句柄 `921428` 存在，`GW_OWNER = null`，前台窗口始终不是对话框。
  - 补丁后：对话框句柄 `986918`，`GW_OWNER = 2624150`（= Harness 主窗口句柄）。
  - 端到端（真实 HTTP `host.pickDirectory` → driver → worker → Win32）：对话框 owner = Harness 窗口 `2624150`，选择器正常返回 `{path:null}`（取消）。
- **修复（两半）**：
  1. `patches/dsh-host-directory-picker-native/owner-pid.cjs`：driver spawn worker 时注入 `DSH_DIALOG_OWNER_PID`，默认 `process.ppid`（桌面壳里就是 Electron 主进程，它拥有 Harness 窗口）；外部预置同名变量可覆盖，便于调试/测试。
  2. `patches/dsh-host-directory-picker-native/owner-window.cjs`：worker 用 koffi 的 `EnumWindows` + `GetWindowThreadProcessId` + `IsWindowVisible` + `GetWindow(GW_OWNER)` 找出该进程的顶层可见无主窗口，作为 `Show(owner)` 的 owner。**取不到就传 `null`**（例如终端里跑 `dsh web`），行为与原来完全一致。
- **为什么"刚下载完"最容易中**：刚下载完时用户是从浏览器/资源管理器启动的，前台进程不是 Harness；等用户真正在应用里点过几轮之后，Windows 的前台规则往往就放行了——所以典型表现是时好时坏。
- **顺带修掉孤儿对话框**：`main.js` 退出时改用 `taskkill /PID <server> /T /F` 杀**整棵进程树**。以前只 `kill()` 直接子进程，对话框 worker（孙进程）会变成孤儿并把模态对话框留在屏幕上，下一次运行就更像"选择器坏了"。

---

### 4.9 提交：API 请求失败不再直接中断任务（提高重试预算）

- **症状**：任务跑到一半报 `DeepSeek API request to https://api.deepseek.com failed`，整轮直接终止。
- **原因**：这是适配器的 `TRANSPORT` 错误（fetch 本身失败）。`TRANSPORT` **本来就在**默认可重试码里，但官方默认策略是 `maxRetries: 2` + 500ms/1s 退避：**总容错只有约 1.5 秒**，网络抖一下额度就用完了。
- **修复**：`patches/dsh-base/llm-retry-policy.cjs` 给 `dsh-base/cordis.patch.yml` 的 `llm-deepseek` 条目补 `config.retryPolicy`：`maxRetries: 8`、`initialDelayMs: 1000`、`maxDelayMs: 30000`、`jitterRatio: 0.2`（约 2 分钟容错）。`retryableCodes` 故意不写，继续用 vendor 默认集合（`TRANSPORT`/`RATE_LIMIT`/`SERVER`/`TIMEOUT`/`EMPTY_RESPONSE`），vendor 以后加新码也自动生效。
- **仍可自行调**：`~/.dsh/settings.yaml` 的 `llm-deepseek.retryPolicy` 覆盖 entry（settings 层优先级更高）。要"永不放弃"可以写 `mode: always`（无上限重试，只受取消/关闭影响）。
- **校验**：直接调 `resolveRetryPolicy({mode:'normal', maxRetries:8, backoff:{initialDelayMs:1000, maxDelayMs:30000, jitterRatio:0.2}})`，实测返回 `{mode:'normal', maxRetries:8, retryableCodes:[EMPTY_RESPONSE,RATE_LIMIT,SERVER,TIMEOUT,TRANSPORT], initialDelayMs:1000, maxDelayMs:30000, jitterRatio:0.2}`。
- **注意**：这是**入口配置**层，改完要重启应用才生效（settings 层才是热加载的）。

---

## 5. 常用命令

### 桌面版（dsh-desktop）
```powershell
cd "C:\Epan\DS Harness\dsh-desktop"
npm start          # 开发模式运行（npx electron .）
npm run build:win  # 重新打包（生成 Setup + portable）
```
- 换图标：替换/重新生成 `build\icon.ico`（或 `build\icon.png`）后重新打包。
- 自检模型同步：`node scripts/model-sync.test.cjs`
- 给已安装的应用打补丁（改完重启生效）：`node scripts/apply-patches.cjs "$env:LOCALAPPDATA\Programs\DeepSeek Harness\resources\app"`
- 调 dsh API（调试用）：`node scripts/dsh-api.mjs <method> '<json-payload>'`（默认连 127.0.0.1:59344，可 `DSH_PORT=xxx` 覆盖；`workspace.create` 传 `{"path":"C:/xxx"}`，注意用正斜杠避免转义问题）。

### 源码仓库（deepseek-harness）
```powershell
cd "C:\Epan\DS Harness\deepseek-harness"
pnpm install
pnpm run build    # 编译 TS 库 + Web 前端
pnpm dsh web      # 起 Web UI（默认 127.0.0.1:3080）
```

---

## 6. 注意事项（接手必读）

1. **`~/.dsh` 是仓库版与桌面版共享的配置目录**（profiles、settings.yaml、.credentials.yaml、storages）。**不要同时运行 `pnpm dsh web` 和桌面版**，会抢 profile 锁导致诡异问题。
2. **exe 未签名**，首次运行 Windows SmartScreen 会提示"更多信息 → 仍要运行"。
3. **不要改回 asar: true**，不要移除 `--expose-internals`、`ELECTRON_RUN_AS_NODE`、`npmRebuild: false`，不要删 `dependencies` 里扁平化的 peer 包。
4. **npmRebuild: false**：原生模块（node-pty / koffi / sharp）都是 N-API 预编译，无需按 Electron 重编；本机路径含空格 + 缺 VS Spectre 库，重编会失败。
5. 应用自包含，**不依赖系统 Node**（服务用 Electron 内置 Node 跑）；但**调试时**可用系统 node 跑 `node scripts/dsh-api.mjs` 调 API。
6. 调试 GUI 程序：stdout 不可见，写日志到 `os.tmpdir()`（`C:\` 根目录不可写）。
7. 桌面版内嵌 npm 官方包 **dsh 0.1.0-rc.6**；仓库源码是 rc.5，两者 profile 可互认但**别同时跑**。

---

## 7. 当前运行状态（交接时点）

- 运行中：**安装版**（`%LOCALAPPDATA%\Programs\DeepSeek Harness`）单个实例，5 个进程（主/渲染/GPU/工具/服务子进程），服务端口 59344（随机，以实际为准）。
- 已注册工作区：
  ```
  workspaceId: 3852e9a7-b449-4bbe-bc2f-ff9c23c05b62
  path:        C:\Epan\DEEPSEEK WORKSPACE
  title:       DEEPSEEK WORKSPACE
  ```
  新建会话时在工作区列表选 "DEEPSEEK WORKSPACE" 即可。
- `~/.dsh/profiles/node_modules` 符号链接已由新版应用自动指向安装目录，健康。

---

## 8. 后续建议（可选）

1. **给 exe 加数字签名**（如自签/OV 证书），消除 SmartScreen 警告。
2. **打 arm64 版**：`electron-builder --win --arm64`（需对应架构的 node-pty/koffi 预编译，先验证）。
3. **版本跟随**：`@deepseek-ai/dsh` 升级后重跑"收集 peer 依赖"逻辑（npm 包升级可能引入新 peer），再 `npm install && npm run build:win`。
4. **美化**：替换为品牌蓝底图标或自定义图标（改 `build-icon.mjs` 后重新生成）。
5. **可选精简**：当前打包了完整 dsh 插件树（~555MB）。如只需 web 功能，可评估裁剪 `dependencies`，但会牺牲插件能力，不建议轻易做。
